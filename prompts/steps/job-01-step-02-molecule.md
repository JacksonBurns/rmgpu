# job-01/step-02: Molecule wrapper over RDKit

Job: job-01 - Units + molecule layer
Prereq: job-00 done (env + skeleton)
This step is part of that job. The job's overall goal:
Build the molecular layer: everything that represents a chemical structure. Internal representation is RDKit; the RMG adjacency list is a serialization format. This is the substrate every later job uses, so correctness and clean APIs matter more than speed. Deliverables: rmgpu/units.py (pint Quantity) and rmgpu/molecule/ (molecule.py, adjlist.py, atomtype.py, resonance.py, symmetry.py, plus filtration helpers and a partial group matcher if needed).
The job's gate (run by the job's final step):
gates/gate_01.py over the test set (species from examples/rmg/ superminimal + c3h4 + minimal input.py + all entries of a small thermo library): 1. adjlist round-trip: parse(Molecule.to_adjlist()) == Molecule (RDKit isomorphism), and to_adjlist() string-equal to RMG-Py's toAdjlist() (references generated by a small RMG-Py script into gates/baselines/adjlist/). 2. canonical SMILES == RDKit canonical (sanity). 3. isomorphism/substructure on a 20-pair set (incl. "same molecule, different atom order" + substructure-with-degeneracy): match RDKit direct calls (pins the wrapper API). 4. atom types: assign_atom_types() equals RMG-Py's assignment (references into gates/baselines/atomtypes/). 5. resonance sets: rmgpu resonance set == RMG-Py's (SMILES-set equality) for the test molecules. Report: counts (molecules tested, pass/fail per check).

## Context (invariant every step)

- You are a FRESH subagent session; you have no other context. This file plus
  what it points at is everything you need.
- Work in /home/jackson/rmgpu/rmgpu (git, branch main). Reference repos (read-
  only, at /home/jackson/rmgpu): RMG-Py, RMG-database, rmgdb, chemprop_example.
- Interpreter: always the env's python directly,
  /home/jackson/miniforge3/envs/rmgpu/bin/python (created in job 00).
  NEVER install into system python or the active venv.
- ONE subagent at a time; this session MUST NOT spawn subagents. Single heavy
  GPU task at a time on this machine; never kill processes you did not start.
- No Cython, no numba, no QM, no Arkane, no fallback estimators, no second
  reactor backend. SI units internally (J, K, Pa, mol) via pint.
- The existing Chemprop-based estimators in RMG-Py (rmgpy/ml/estimator.py)
  are being REPLACED by rmgpu's new ones - they are reference-only (layout,
  cutoffs, DSL wiring). rmgpu's estimators are re-implemented per
  /home/jackson/rmgpu/chemprop_example/predicting.ipynb. See README.md,
  "The ML estimators are NEW".

## Where you are

- Previous step (already done, its code is in the tree): job-01/step-01-units
- This step: job-01/step-02-molecule
- Next step (do NOT start it): job-01/step-03-adjlist

## Goal

Build the Molecule class: the thin wrapper over RWMol that everything
else uses. Port the SEMANTICS of RMG's Molecule (charge, radicals, formula,
labeled atoms) - NOT the hand-rolled graph code (that is RDKit's job now).

## Reference to read (this step's budget)

  RMG-Py/rmgpy/molecule/translator.py  (595 - RDKit <-> Molecule
    conversion; the core of the wrapper)
  RMG-Py/rmgpy/molecule/molecule.py  (3255 - read the PUBLIC API + label/bond
    mutation semantics + charge/radical/formula handling; do NOT port the
    graph methods (RDKit), symmetry internals (step 9), resonance internals
    (step 8))
  RMG-Py/rmgpy/molecule/element.py  (395 - element table incl.
    symbol_by_number; port as data)
  RMG-Py/rmgpy/molecule/graph.pyx / vf2.pyx: DO NOT PORT - replaced by RDKit

Read ONLY what is listed plus the direct dependencies you hit (note any extra reads in the report). The budget is sized so the listed reads + the deliverables fit ONE session without context compaction - if you find the reads are bigger than that, STOP and record it in the report (a step whose reads overflow is a framework bug, not something to push through).

## Deliverables

- rmgpu/molecule/molecule.py:
    `Molecule` = immutable-by-default (copy-on-write) wrapper holding:
    `rdkit: RWMol` (canonical, kekulized), charge, radical count, atom-type
    assignments (side map; step 7), labels (RMG labeled-atom semantics -
    labels like '1','2','*','*1' and bond labels - the recipe engine in job
    05 mutates bond labels on copies), resonance list (step 8), formula,
    symmetry number (step 9).
    Constructors: from SMILES, InChI, adjacency list (step 6), RWMol.
    API: .to_smiles() .to_inchi() .to_adjlist() .get_formula()
    .get_symmetry_number() (step 9 wires the real number; stub ok now)
    __eq__/__hash__ via RDKit canonical form; .is_isomorph(other);
    .is_substructure(other); .substructure_match_count(other) (degeneracy,
    via GetSubstructMatch(es)); labeled-atom accessors + copy-with-labels;
    charge/radical accessors.
    Isomorphism/substructure go through RDKit, NOT a re-implemented VF2.
- tests/test_molecule.py: SMILES/InChI/adjlist(round-trip via placeholder)
  construction; eq/hash; isomorphism on permuted-atom-order pairs;
  substructure degeneracy on classic cases (e.g. benzene ring in toluene);
  label copy semantics (mutating a copy leaves the original intact).

## Checks (must run and pass before you claim done)

  pytest tests/test_molecule.py -q -> all pass
  spot: Molecule("CC").to_smiles() is RDKit canonical; eq across
  constructions (SMILES vs InChI)

## Pitfalls

- If you find yourself porting graph operations (isomorphism,
  kekulize, substructure), STOP - use RDKit.
- The labeled-atom semantics must be faithful: job 05's recipe engine
  depends on exact label behavior ('1' vs '*1', bond labels,
  _relabelAtoms). Where the step-2 read leaves an API gap, add the minimum
  to this module and note it in the report.

## Done protocol (exact)

1. Commit the code: `git commit -am "job-01/step-02: <summary>"` (several commits are fine; NO push).
2. STATUS.md: set your step row to `done` and append a session-log entry: `### <date> - job-01/step-02 / built: ... / checks: ... (GREEN|RED + one line) / commits: <hashes> / next: <the next step id>`. Do NOT set the top-level NEXT pointer - the coordinator does.
3. Write the report to reports/job-01-step-02-molecule.md with: what was built (files + ~1 line each), the checks run (the commands + the real results, not a paraphrase), the reference reads beyond the list (if any), the deviations from this file (if any, with the cause), and what the next step should know first.
4. STOP. Do not start the next step. Do not spawn subagents.
