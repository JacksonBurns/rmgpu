# job-01/step-06: Symmetry numbers + optical isomers + filtration

Job: job-01 - Units + molecule layer
Prereq: job-00 done (env + skeleton)
This step is part of that job. The job's overall goal:
Build the molecular layer: everything that represents a chemical structure. Internal representation is RDKit; the RMG adjacency list is a serialization format. This is the substrate every later job uses, so correctness and clean APIs matter more than speed. Deliverables: rmgpu/units.py (pint Quantity) and rmgpu/molecule/ (molecule.py, adjlist.py, atomtype.py, resonance.py, symmetry.py, plus filtration helpers and a partial group matcher if needed).
The job's gate (run by the job's final step):
gates/gate_01.py over the test set (species from examples/rmg/ superminimal + c3h4 + minimal input.py + all entries of a small thermo library): 1. adjlist round-trip: parse(Molecule.to_adjlist()) == Molecule (RDKit isomorphism), and to_adjlist() string-equal to RMG-Py's toAdjlist() (references generated by a small RMG-Py script into gates/baselines/adjlist/). 2. canonical SMILES == RDKit canonical (sanity). 3. isomorphism/substructure on a 20-pair set (incl. "same molecule, different atom order" + substructure-with-degeneracy): match RDKit direct calls (pins the wrapper API). 4. atom types: assign_atom_types() equals RMG-Py's assignment (references into gates/baselines/atomtypes/). 5. resonance sets: rmgpu resonance set == RMG-Py's (SMILES-set equality) for the test molecules. Report: counts (molecules tested, pass/fail per check).

## Context (invariant every step)

- You are a FRESH human-started session; you have no other context. This file plus
  what it points at is everything you need.
- Work in /home/jackson/rmgpu/rmgpu (git, branch main). Reference repos (read-
  only, at /home/jackson/rmgpu): RMG-Py, RMG-database, rmgdb, chemprop_example.
- Interpreter: always the env's python directly,
  /home/jackson/miniforge3/envs/rmgpu/bin/python (created in job 00).
  NEVER install into system python or the active venv.
- ONE session at a time; this session MUST NOT spawn subagents. Single heavy
  GPU task at a time on this machine; never kill processes you did not start.
- No Cython, no numba, no QM, no Arkane, no fallback estimators, no second
  reactor backend. SI units internally (J, K, Pa, mol) via pint.
- The existing Chemprop-based estimators in RMG-Py (rmgpy/ml/estimator.py)
  are being REPLACED by rmgpu's new ones - they are reference-only (layout,
  cutoffs, DSL wiring). rmgpu's estimators are re-implemented per
  /home/jackson/rmgpu/chemprop_example/predicting.ipynb. See README.md,
  "The ML estimators are NEW".

## Where you are

- Previous step (already done, its code is in the tree): job-01/step-05-resonance
- This step: job-01/step-06-symmetry
- Next step (do NOT start it): job-01/step-07-gate

## Goal

Port symmetry-number / optical-isomer counting (used by statmech in
job 07 for conformer factors) and the forbidden-structure filtration
matching (SMARTS-based; the `forbidden:` input block in job 03 needs it).

## Reference to read (this step's budget)

  RMG-Py/rmgpy/molecule/symmetry.py  (568 - symmetry numbers +
    optical isomers; used by statmech later)
  RMG-Py/rmgpy/molecule/filtration.py  (459 - forbidden-structure / filter
    matching, SMARTS-based)
  RMG-Py/rmgpy/molecule/inchi.py (skim - delegate to RDKit)

Read ONLY what is listed plus the direct dependencies you hit (note any extra reads in the report). The budget is sized so the listed reads + the deliverables fit ONE session without context compaction - if you find the reads are bigger than that, STOP and record it in the report (a step whose reads overflow is a framework bug, not something to push through).

## Deliverables

- rmgpu/molecule/symmetry.py: get_symmetry_number(Molecule) and
  optical-isomer counting per RMG's semantics; wire
  Molecule.get_symmetry_number() (step 5 stub) to the real implementation.
- rmgpu/molecule/filtration.py: forbidden-structure matching - given
  forbidden structures (from the input's `forbidden:` block, job 03) and a
  Molecule, return the match info per RMG's filtration semantics (SMARTS
  matching via RDKit, RMG's reporting).
- tests: symmetry numbers on a 20-molecule set vs RMG-Py reference
  (gates/baselines/symmetry/); filtration on 10 cases (incl. a
  forbidden-aromatic case).

## Checks (must run and pass before you claim done)

  pytest tests/test_symmetry.py tests/test_filtration.py -q -> pass

## Pitfalls

- Symmetry numbers feed the DoS (job 07) - a wrong number silently
  shifts k(T,P); the gate (step 10) baselines them.
- Filtration is a MATCHER, not a filter pipeline - job 03/06 call it with
  the user's forbidden list.

## Done protocol (exact)

1. Commit the code: `git commit -am "job-01/step-06: <summary>"` (several commits are fine; NO push).
2. STATUS.md: set your step row to `done` and append a session-log entry: `### <date> - job-01/step-06 / built: ... / checks: ... (GREEN|RED + one line) / commits: <hashes> / next: <the next step id>`. Update the top-level NEXT pointer to the next step's file.
3. Write the report to reports/job-01-step-06-symmetry.md with: what was built (files + ~1 line each), the checks run (the commands + the real results, not a paraphrase), the reference reads beyond the list (if any), the deviations from this file (if any, with the cause), and what the next step should know first.
4. STOP. Do not start the next step. Do not spawn subagents.
