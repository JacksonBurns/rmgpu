# job-01: Units + molecule layer

Status of this file: a JOB BRIEF, not a task. Do not try to "do this job"
in one session - it is decomposed into the steps below, one session each.
The coordinator (see README.md, "Session vs step") picks the next step from
STATUS.md's NEXT pointer; this file is the map of the job.

## Goal

Build the molecular layer: everything that represents a chemical structure. Internal representation is RDKit; the RMG adjacency list is a serialization format. This is the substrate every later job uses, so correctness and clean APIs matter more than speed. Deliverables: rmgpu/units.py (pint Quantity) and rmgpu/molecule/ (molecule.py, adjlist.py, atomtype.py, resonance.py, symmetry.py, plus filtration helpers and a partial group matcher if needed).

## Prereq

job-00 done (env + skeleton)

## Steps (strictly sequential; one fresh subagent session each)

  step 01  prompts/steps/job-01-step-01-units.md  units.py - pint-backed Quantity
  step 02  prompts/steps/job-01-step-02-molecule.md  Molecule wrapper over RDKit
  step 03  prompts/steps/job-01-step-03-adjlist.md  Adjacency-list parser/serializer (byte-compatible)
  step 04  prompts/steps/job-01-step-04-atomtype.md  Atom-type DB + assignment
  step 05  prompts/steps/job-01-step-05-resonance.md  Resonance structure generation
  step 06  prompts/steps/job-01-step-06-symmetry.md  Symmetry numbers + optical isomers + filtration
  step 07  prompts/steps/job-01-step-07-gate.md  Job-01 gate (round-trips vs RMG-Py)

## The job gate

Run by the final step's session (gates/gate_01.py, report
reports/job-01.md):

gates/gate_01.py over the test set (species from examples/rmg/ superminimal + c3h4 + minimal input.py + all entries of a small thermo library): 1. adjlist round-trip: parse(Molecule.to_adjlist()) == Molecule (RDKit isomorphism), and to_adjlist() string-equal to RMG-Py's toAdjlist() (references generated by a small RMG-Py script into gates/baselines/adjlist/). 2. canonical SMILES == RDKit canonical (sanity). 3. isomorphism/substructure on a 20-pair set (incl. "same molecule, different atom order" + substructure-with-degeneracy): match RDKit direct calls (pins the wrapper API). 4. atom types: assign_atom_types() equals RMG-Py's assignment (references into gates/baselines/atomtypes/). 5. resonance sets: rmgpu resonance set == RMG-Py's (SMILES-set equality) for the test molecules. Report: counts (molecules tested, pass/fail per check).

## When the job is done

The final step's report (reports/job-01.md) has the gate result, the
job table row is `done` (or `blocked` with the cause), and the NEXT pointer
in STATUS.md targets job-02's first step (if there is a next job).
